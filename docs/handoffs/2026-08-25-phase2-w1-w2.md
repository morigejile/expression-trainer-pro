# Phase 2 W1-W2 新会话 Handoff

> **Status: Historical / Superseded.** 本文只保留 2026-08-25 的任务拆分历史。
> 双人审核、BM-03 阻塞、50～100 条选择和旧安全门禁已失效，不得据此继续实施。
> 当前范围以 `codex/benchmark/bm01-dataset` 的 2026-08-26 convergence handoff、Roadmap 与后继提交为准。

## 可直接粘贴到新会话的总控指令

```text
继续处理 expression-trainer-pro 的 Phase 2 / W1-W2，并按三条独立工作线推进。

仓库：D:\Codex_projects\expression-trainer-pro
精确基线：94e192d73c04ec36d5c4ad016e8e5daf1dc4670d
基线含义：origin/main 已合并 Phase 0、Phase 1 和 T-08；Electron 为 43.4.1。

固定开发运行时：
- Node：C:\Users\mr\AppData\Local\hermes\node\node.exe
- npm：C:\Users\mr\AppData\Local\hermes\node\npm.cmd
- Node 22.23.0、npm 12.0.2、Windows x64

重要现场约束：
- 主工作树当前有用户改动：README.md 已修改，docs/onboarding.md 未跟踪。
- 不得清理、覆盖、暂存或提交这两项用户改动。
- 不直接在主工作树实施 BM 任务；从精确基线创建独立 worktree。
- 创建前检查目标 branch/worktree 是否已存在。
- 不使用 git reset --hard、git checkout --、git clean 或递归删除。
- 未经我另行要求：不 merge main、不 push、不创建 PR、不删除 worktree。
- 每个 commit 使用英文标题和简短中文正文。

开始前必须阅读：
- docs/roadmap.md 的 Phase 2 和 Phase 3
- docs/architecture/adr/0005-select-default-asr-model-by-benchmark.md
- docs/requirements/requirements.md
- docs/development.md
- 对应 implementation plan

这些 plan 当前位于主工作树 D:\Codex_projects\expression-trainer-pro\docs\superpowers\plans\；请在创建精确 SHA worktree 前读取。新 worktree 不一定包含尚未提交的协调文档，不得因此改用脏主工作树实施。

并行工作线：

1. W1-A / BM-01 数据集契约与语料治理
   - 分支：codex/benchmark/bm01-dataset
   - worktree：D:\Codex_projects\expression-trainer-pro-bm01
   - 计划：docs/superpowers/plans/2026-08-25-bm01-benchmark-dataset.md

2. W1-B / BM-03 音频采样率基线
   - 分支：codex/benchmark/bm03-audio-baseline
   - worktree：D:\Codex_projects\expression-trainer-pro-bm03
   - 计划：docs/superpowers/plans/2026-08-25-bm03-audio-baseline.md

3. W2-P / BM-04～06 候选预备
   - 分支：codex/benchmark/model-candidate-prep
   - worktree：D:\Codex_projects\expression-trainer-pro-model-prep
   - 可立即与 W1 同步启动，只做候选清单、来源、许可证、hash、配置和本地加载验证。
   - 计划：docs/superpowers/plans/2026-08-25-bm04-bm06-candidate-prep.md

BM-02 的启动门禁：
- BM-01 分支先产生并提交 Contract Gate：manifest schema、validator、最小无隐私 fixture 和通过的单元测试。
- Contract Gate 提交产生后，从该提交创建 codex/benchmark/bm02-harness 和独立 worktree。
- BM-02 可以在完整 50～100 条语料尚在收集时开发，但不得在 BM-01 完成前标记 Completed 或合入 main。
- BM-02 计划：docs/superpowers/plans/2026-08-25-bm02-reproducible-harness.md

禁止提前做的事情：
- 不运行或发布 BM-04～06 正式排名。
- 不在 D-01 冻结产品权重前查看或使用汇总排名做决策。
- 不改生产 ASR、AudioWorklet、IPC、Main 执行边界或默认模型。
- 不引入 Python/PyTorch、Forge、electron-builder 或新的默认运行依赖。
- 不把未授权录音、个人身份信息、API Key 或本机绝对语料路径提交到 Git。

协调门禁：
- Gate A：BM-01 Contract Gate 通过后，启动 BM-02 stacked worktree。
- Gate B：BM-01 全量验收后，BM-02 才可完成并准备合流。
- Gate C：BM-02、BM-03 完成且 D-01 权重冻结后，才允许正式执行 BM-04～06。
- 正式模型测量必须独占同一 benchmark 机器串行运行；模型下载、适配和 dry-run 可以并行。

每条线都先跑基线：
& 'C:\Users\mr\AppData\Local\hermes\node\npm.cmd' ci
& 'C:\Users\mr\AppData\Local\hermes\node\npm.cmd' test
& 'C:\Users\mr\AppData\Local\hermes\node\npm.cmd' run check
git diff --check

实施时使用 TDD；每个小任务独立提交。完成后进行独立审查并汇报：分支、worktree、最终 SHA、测试结果、修改文件、证据边界、未验证事项、主工作树和其他工作树未被修改的证明。
```

## 并行关系说明

W1 两条线可以完全并行；W2 不是整体等待 W1：

```text
BM-01 ── Contract Gate ──→ BM-02 开发 ── BM-01 完成 ──→ BM-02 完成
  │
  └──────────────────────────────────────────────────────────────┐
BM-03 ────────────────────────────────────────────────────────────┼─→ BM-04 正式对照
BM-04～06 候选预备 ───────────────────────────────────────────────┘
                                      D-01 冻结后才允许正式排名
```

因此当前最大有效并发是三条：BM-01、BM-03、候选预备。BM-02 在 BM-01 Contract Gate 后接替或增加为第四条工作线；如果只有三份工程容量，优先暂停候选预备，把容量交给 BM-02。

## 如需拆成三个独立新会话

### W1-A / BM-01

```text
执行 expression-trainer-pro 的 BM-01。仓库基线、运行时、安全约束和交付格式以 D:\Codex_projects\expression-trainer-pro\docs\handoffs\2026-08-25-phase2-w1-w2.md 为准；完整计划是 D:\Codex_projects\expression-trainer-pro\docs\superpowers\plans\2026-08-25-bm01-benchmark-dataset.md。使用 codex/benchmark/bm01-dataset 和 D:\Codex_projects\expression-trainer-pro-bm01。先交付并提交 Contract Gate，再继续真实语料治理；Contract Gate SHA 要单独汇报给 BM-02。不要触碰主工作树的 README.md 和 docs/onboarding.md，不 merge/push/PR/清理 worktree。
```

### W1-B / BM-03

```text
执行 expression-trainer-pro 的 BM-03。仓库基线、运行时、安全约束和交付格式以 D:\Codex_projects\expression-trainer-pro\docs\handoffs\2026-08-25-phase2-w1-w2.md 为准；完整计划是 D:\Codex_projects\expression-trainer-pro\docs\superpowers\plans\2026-08-25-bm03-audio-baseline.md。使用 codex/benchmark/bm03-audio-baseline 和 D:\Codex_projects\expression-trainer-pro-bm03。只建立隔离 probe 和采样率证据，不修生产 Audio/ASR 链路；无真实 44.1/48 kHz 证据时不得标记 Completed。不要触碰主工作树的 README.md 和 docs/onboarding.md，不 merge/push/PR/清理 worktree。
```

### W2 / 候选预备与 BM-02 门禁

```text
协调 expression-trainer-pro 的 W2。仓库基线、运行时、安全约束和交付格式以 D:\Codex_projects\expression-trainer-pro\docs\handoffs\2026-08-25-phase2-w1-w2.md 为准。立即执行候选预备计划 D:\Codex_projects\expression-trainer-pro\docs\superpowers\plans\2026-08-25-bm04-bm06-candidate-prep.md，使用 codex/benchmark/model-candidate-prep 和 D:\Codex_projects\expression-trainer-pro-model-prep。同步监测 BM-01 的 Contract Gate；收到其提交 SHA 后，严格从该 SHA 创建 codex/benchmark/bm02-harness 和独立 worktree，并执行 D:\Codex_projects\expression-trainer-pro\docs\superpowers\plans\2026-08-25-bm02-reproducible-harness.md。候选预备只做来源、许可证、hash、配置、native load 和 dry-run；不得产生正式排名。BM-01 未完成前 BM-02 不得标记 Completed 或合入 main。不要触碰主工作树用户改动，不 merge/push/PR/清理 worktree。
```
